[package]
name = "cli-nmap"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
pnet = "0.35"
pnet_datalink = "0.35"
rand = "0.8"
chrono = { version = "0.4", features = ["serde"] }
